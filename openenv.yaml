name: delivery-route-env

version: "1.0"

description: Delivery Route Optimization Environment

entrypoint: env.environment:DeliveryEnv

api:
  reset: /reset
  step: /step
  state: /state

tasks:
  - tasks.easy:get_task
  - tasks.medium:get_task
  - tasks.hard:get_task

grader: graders.grader:grade